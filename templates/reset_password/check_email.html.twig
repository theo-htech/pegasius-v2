{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - {{ 'reset_password_email.title' | trans }}{% endblock %}

{% block body %}
    <section class="h-100 gradient-form" style="background-color: #eee;">
        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col-xl-10">
                    <div class="card rounded-3 text-black">
                        <div class="row g-0">
                            <div class="col-lg-6">
                                <div class="card-body p-md-5 mx-md-4">

                                    <div class="text-center">
                                        <h4 class="mt-1 mb-2 pb-1">O.do.P</h4>
                                    </div>
                                    <p>
                                        {{ 'reset_password_email.info' | trans }}
                                    </p>
                                    <p> {{ 'reset_password_email.info_retry' | trans }}
                                        <a href="{{ path('app_forgot_password_request') }}">{{ 'reset_password_email.info_retry_link' | trans }}</a></p>
                                </div>
                            </div>
                            {{ include('public_template/login_register_img.html.twig') }}
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
